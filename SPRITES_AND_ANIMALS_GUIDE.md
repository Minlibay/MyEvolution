# 🎨 Руководство по спрайтам и животным

## Обзор новых функций

Добавлены следующие возможности:
1. **Спрайты объектов** — визуальное отображение ресурсов на карте
2. **Текстуры земли и биомов** — красивый фон вместо чёрного экрана
3. **Система животных** — создание и управление животными с 4 спрайтами направлений

---

## 1. Спрайты объектов на карте

### Что изменилось
Цветные квадраты объектов заменены на PNG-спрайты:
- 🌳 **Дерево** (wood) → `tree.png`
- 🪨 **Камень** (stone) → `stone.png`
- 🌿 **Растение** (plant) → `flans.png`
- 🫐 **Ягоды** (berry) → `berry.png`
- 🦴 **Кости** (bone) → `bones.png`
- 🧵 **Волокно** (fiber) → `fiber.png`

### Расположение спрайтов
```
evolution_simulation/src/web/static/sprites/
├── tree.png
├── stone.png
├── flans.png
├── berry.png
├── Bones.png
├── fiber.png
├── male.png
├── female.png
└── rip.png
```

### Как заменить спрайты
1. Подготовьте PNG файл (рекомендуемый размер: 32×32 или 64×64 px)
2. Замените соответствующий файл в папке `src/web/static/sprites/`
3. Перезагрузите страницу (Ctrl+F5 для сброса кэша)

---

## 2. Текстуры земли и биомов

### Доступные слоты текстур

| Слот | Описание | Использование |
|------|----------|---------------|
| `ground` | **Основная земля** | Обязательная текстура, заменяет чёрный фон |
| `ground_forest` | Лесной биом | Для будущих зон леса |
| `ground_desert` | Пустыня | Для пустынных зон |
| `ground_mountain` | Горы | Для горных зон |
| `ground_swamp` | Болото | Для болотистых зон |
| `ground_winter` | Зимняя земля (снег) | Для зимнего сезона |
| `ground_winter_forest` | Зимний лес | Зимний вариант леса |
| `ground_winter_desert` | Зимняя пустыня | Зимний вариант пустыни |

### Требования к текстурам
- **Формат:** PNG (прозрачность поддерживается)
- **Размер файла:** до 2 МБ
- **Рекомендуемый размер:** 64×64 px или 128×128 px
- **Тайлинг:** текстура повторяется (tiled) по всей карте

### Как загрузить текстуру

#### Через админку (рекомендуется)
1. Откройте админ-панель: `/adminkins`
2. Войдите как администратор
3. Перейдите на вкладку **🎨 Текстуры**
4. Выберите слот (например, `ground`)
5. Нажмите **Выбрать файл** → выберите PNG
6. Нажмите **Загрузить**
7. Текстура появится на карте автоматически

#### Через API (для разработчиков)
```bash
# Загрузка текстуры
curl -X POST "http://localhost:8000/api/admin/sprites/ground?token=YOUR_TOKEN" \
  -F "file=@ground_texture.png"

# Получение текстуры
curl "http://localhost:8000/api/sprites/ground" -o ground.png

# Удаление текстуры
curl -X DELETE "http://localhost:8000/api/admin/sprites/ground?token=YOUR_TOKEN"

# Список всех слотов
curl "http://localhost:8000/api/sprites"
```

### Где хранятся текстуры
```
evolution_simulation/data/sprites/
├── ground.png
├── ground_forest.png
├── ground_desert.png
└── ...
```

---

## 3. Система животных

### Возможности
- Создание животных с характеристиками (HP, урон, броня, скорость, агрессия)
- Загрузка 4 спрайтов направлений (вверх, вниз, влево, вправо)
- Управление статусом (активно, пассивное, враждебное, дружелюбное, отключено)
- Полное CRUD управление через админку

### Характеристики животного

| Параметр | Тип | Диапазон | Описание |
|----------|-----|----------|----------|
| `name` | string | 1-60 символов | Название животного |
| `status` | enum | active/passive/hostile/friendly/disabled | Статус поведения |
| `health` | int | 1-10000 | Здоровье (HP) |
| `speed` | float | 0.1-10.0 | Скорость передвижения |
| `damage` | int | 0-1000 | Урон при атаке |
| `armor` | int | 0-1000 | Броня (защита) |
| `aggression` | float | 0.0-1.0 | Агрессивность (0 = мирное, 1 = очень агрессивное) |
| `description` | string | 0-200 символов | Краткое описание |

### Как создать животное

#### Через админку
1. Откройте `/adminkins` → вкладка **🐾 Животные**
2. Заполните форму:
   - Название: например, "Волк"
   - Статус: выберите из списка
   - Характеристики: HP, урон, броня, скорость, агрессия
   - Описание (опционально)
3. Нажмите **Создать животное**
4. Животное появится в списке ниже

#### Загрузка спрайтов направлений
1. В карточке животного найдите 4 блока направлений (Вверх, Вниз, Влево, Вправо)
2. Для каждого направления:
   - Нажмите **Выбрать файл**
   - Выберите PNG файл (рекомендуемый размер: 32×32 или 48×48 px)
   - Нажмите кнопку **↑** (загрузить)
3. Спрайт появится в превью

#### Через API
```bash
# Создание животного
curl -X POST "http://localhost:8000/api/admin/animals?token=YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Волк",
    "status": "hostile",
    "health": 150,
    "speed": 1.5,
    "damage": 25,
    "armor": 10,
    "aggression": 0.8,
    "description": "Опасный хищник"
  }'

# Загрузка спрайта направления
curl -X POST "http://localhost:8000/api/admin/animals/ANIMAL_ID/sprite/up?token=YOUR_TOKEN" \
  -F "file=@wolf_up.png"

# Получение списка животных
curl "http://localhost:8000/api/animals"

# Получение спрайта
curl "http://localhost:8000/api/animals/ANIMAL_ID/sprite/up" -o wolf_up.png

# Удаление животного
curl -X DELETE "http://localhost:8000/api/admin/animals/ANIMAL_ID?token=YOUR_TOKEN"
```

### Где хранятся данные животных
```
evolution_simulation/data/animals/
├── animals.json                    # Метаданные всех животных
├── animal_1234567890_5678/
│   ├── up.png
│   ├── down.png
│   ├── left.png
│   └── right.png
└── animal_1234567891_9012/
    └── ...
```

---

## API Endpoints

### Спрайты текстур
- `GET /api/sprites` — список слотов и статус загрузки
- `GET /api/sprites/{slot}` — получить текстуру
- `POST /api/admin/sprites/{slot}` — загрузить текстуру (admin)
- `DELETE /api/admin/sprites/{slot}` — удалить текстуру (admin)

### Животные
- `GET /api/animals` — список всех животных
- `GET /api/animals/{id}` — получить данные животного
- `POST /api/admin/animals` — создать животное (admin)
- `PUT /api/admin/animals/{id}` — обновить животное (admin)
- `DELETE /api/admin/animals/{id}` — удалить животное (admin)
- `POST /api/admin/animals/{id}/sprite/{direction}` — загрузить спрайт (admin)
- `GET /api/animals/{id}/sprite/{direction}` — получить спрайт

---

## Примеры использования

### Пример 1: Создание волка с полным набором спрайтов

```python
import requests

TOKEN = "your_admin_token"
BASE_URL = "http://localhost:8000"

# 1. Создаём животное
response = requests.post(
    f"{BASE_URL}/api/admin/animals?token={TOKEN}",
    json={
        "name": "Серый волк",
        "status": "hostile",
        "health": 150,
        "speed": 1.8,
        "damage": 30,
        "armor": 15,
        "aggression": 0.85,
        "description": "Опасный хищник, охотится стаями"
    }
)
animal = response.json()["animal"]
animal_id = animal["id"]

# 2. Загружаем спрайты для всех направлений
directions = ["up", "down", "left", "right"]
for direction in directions:
    with open(f"wolf_{direction}.png", "rb") as f:
        requests.post(
            f"{BASE_URL}/api/admin/animals/{animal_id}/sprite/{direction}?token={TOKEN}",
            files={"file": f}
        )

print(f"Волк создан! ID: {animal_id}")
```

### Пример 2: Загрузка текстуры травы

```python
import requests

TOKEN = "your_admin_token"
BASE_URL = "http://localhost:8000"

# Загружаем текстуру основной земли
with open("grass_texture.png", "rb") as f:
    response = requests.post(
        f"{BASE_URL}/api/admin/sprites/ground?token={TOKEN}",
        files={"file": f}
    )

if response.ok:
    print("Текстура загружена! Обновите страницу симуляции.")
else:
    print(f"Ошибка: {response.json()}")
```

---

## Советы и рекомендации

### Для текстур земли
1. **Используйте seamless (бесшовные) текстуры** — они лучше тайлятся
2. **Оптимизируйте размер** — 64×64 или 128×128 px достаточно
3. **Тестируйте на разных масштабах** — используйте зум на карте
4. **Сохраняйте оригиналы** — на случай если нужно вернуться

### Для спрайтов животных
1. **Единый стиль** — все 4 направления должны быть в одном стиле
2. **Прозрачный фон** — используйте PNG с альфа-каналом
3. **Размер 32×32 или 48×48 px** — оптимально для карты
4. **Анимация** — пока не поддерживается, но можно добавить в будущем

### Для объектов
1. **Сохраняйте пропорции** — спрайты масштабируются автоматически
2. **Используйте контрастные цвета** — чтобы объекты выделялись на фоне
3. **Тестируйте на разных биомах** — если планируете использовать разные текстуры земли

---

## Устранение неполадок

### Текстура не отображается
- Проверьте, что файл PNG корректный (откройте в редакторе)
- Убедитесь, что размер файла ≤ 2 МБ
- Очистите кэш браузера (Ctrl+Shift+Delete)
- Проверьте консоль браузера на ошибки (F12)

### Спрайт животного не загружается
- Убедитесь, что направление указано правильно: `up`, `down`, `left`, `right`
- Проверьте формат файла (только PNG)
- Проверьте права администратора
- Посмотрите логи сервера на ошибки

### Животное не появляется в игре
- Система животных пока только для управления данными
- Интеграция с симуляцией будет добавлена в следующих версиях
- Сейчас можно создавать и хранить животных с их характеристиками

---

## Будущие улучшения

Планируется добавить:
- [ ] Интеграция животных в симуляцию (AI, движение, взаимодействие с агентами)
- [ ] Анимация спрайтов (несколько кадров для каждого направления)
- [ ] Биомы на карте (автоматическая смена текстур по зонам)
- [ ] Сезонная смена текстур (зима/лето)
- [ ] Редактор животных в админке (изменение характеристик)
- [ ] Импорт/экспорт животных (JSON)

---

## Поддержка

Если возникли вопросы или проблемы:
1. Проверьте логи сервера: `python -m src.web.app`
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь, что у вас права администратора
4. Проверьте, что все файлы в правильных директориях

**Версия:** 1.0  
**Дата:** Март 2026
